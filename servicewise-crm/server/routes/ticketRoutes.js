import { Router } from "express"; import { createTicket, deleteTicket, getTicketById, getTickets, updateTicket } from "../controllers/ticketController.js"; import { requireApiKey } from "../middleware/authMiddleware.js";
const router = Router(); router.get("/", requireApiKey, getTickets); router.get("/:id", requireApiKey, getTicketById); router.post("/", requireApiKey, createTicket); router.put("/:id", requireApiKey, updateTicket); router.delete("/:id", requireApiKey, deleteTicket); export default router;
